db:
  image: tutum/mariadb
  volumes:
    - /data/Downloads/tmp/mdb:/var/lib/mysql
  environment:
    - MARIADB_PASS=somepass
  ports:
    - "3306"

web:
  build: .
  volumes:
    - /var/repo
    - /data/Downloads/tmp/config:/config
  ports:
    - "80:80"
  links:
    - db
